import { EntityPage } from "@/components/EntityPage"; export default async function Page({params}:{params:Promise<{slug:string}>}){return <EntityPage type="scholarships" slug={(await params).slug}/>;}
