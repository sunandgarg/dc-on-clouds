import type { Metadata } from "next";
export const metadata: Metadata = { title: "Sign in", robots: { index: false, follow: false } };
export default function AuthPage() { return <main className="container page"><div className="answer-grid" style={{gridTemplateColumns:"minmax(0,480px)",justifyContent:"center"}}><article><p className="eyebrow">Secure account</p><h1>Sign in to DekhoCampus</h1><form action="/api/auth/login" method="post" className="search"><label htmlFor="email">Email</label><div><input id="email" type="email" name="email" autoComplete="email" required /></div><label htmlFor="password">Password</label><div><input id="password" type="password" name="password" autoComplete="current-password" required /></div><button type="submit">Sign in</button></form><p>Your credentials are sent to the Spring API and tokens are stored in secure HTTP-only cookies.</p></article></div></main>; }
